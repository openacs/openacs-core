<sect1 id="oracle">
<title>Installing Oracle 8.1.6</title>

<para><emphasis role="strong">NOTE:</emphasis><emphasis>
This document was written based on Oracle, version 8.1.6. 
However, the same approach should lead to a successful installation of
Oracle 8.1.7. If you encounter any incompatibilities, please
<ulink url="mailto:acs-docs@arsdigita.com">let us know</ulink></emphasis></para>

<sect2 id="install-oracle-getit">
<title>Acquire Oracle 8.1.6 Enterprise Edition</title>

<para>You can obtain the software through a variety of methods:</para>

<orderedlist>
<listitem><para>Order a CD from the <ulink url="http://store.oracle.com/">Oracle Store</ulink>.
There is a link under the heading <emphasis>Buying Tips</emphasis> that says &quot;Learn
how to trial Oracle software.&quot; Click this for instructions on how to
make your order. The cost is currently $39.95 with delivery estimated between
3-4 business days. 


</para></listitem>

<listitem><para>Request a free demonstration CD. At the <ulink url="http://technet.oracle.com/software/products/oracle8i/software_index.htm">
Oracle Downloads</ulink> page, click on the appropriate version of Oracle 8.1.6
Release 2 for your OS. You should see a &quot;Try Online&quot; link. After
filling out some information forms, you should be able to order a version of
the CD. This takes about 2-3 weeks for delivery. 


</para></listitem>

<listitem><para>You can download Oracle from the Technet <ulink url="ftp://ftp.oracle.com/pub/www/otn/">ftp site</ulink>. The Linux version is
at <ulink url="ftp://ftp.oracle.com/pub/www/otn/linux/oracle8i/oracle8161_tar.gz">ftp://ftp.oracle.com/pub/www/otn/linux/oracle8i/oracle8161_tar.gz</ulink></para>
<para>Note that the Oracle tarball that you will download is huge (&gt;
250MB).</para>



<itemizedlist>
<listitem><para>Oracle 8.1.6 now comes with a Java RunTime Environment built-in to the
distribution, so you no longer have to download and install it separately. 


</para></listitem>

<listitem><para>After the download is complete, untar the file to a convenient location.
To do this, you will need to login and cd to the directory where the archive
is. </para>

<programlisting>
$ cd /directory/where/oracle/is
$ tar -xzvf oracle8161_tar.gz
       
</programlisting>
</listitem>
</itemizedlist>
</listitem>
</orderedlist>


</sect2>

<sect2 id="install-oracle-keepinmind">
<title>Things to Keep in Mind</title>


<para>Throughout these instructions, we will refer to a number of configurable
settings and advise certain defaults. With the exception of passwords, we
advise you to follow these defaults unless you know what you are doing.
Subsequent documents will expect that you used the defaults, so a change made
here will necessitate further changes later. For a guide to the defaults,
please see <xref linkend="install-oracle-defaults"/>.</para>



</sect2>

<sect2 id="install-oracle-preinstall">
<title>Pre-Installation Tasks</title>


<para>Though Oracle 8.1.6 has an automated installer, we still need to perform
several manual, administrative tasks before we can launch it. You must
perform all of these steps as the <phrase>root</phrase> user. We recommend
entering the X window system as a user other than <phrase>root</phrase> and
then doing a <computeroutput>su -</computeroutput>. This command gives you full root access.</para>



<itemizedlist>
<listitem><para>Login in as a non-root user 


</para></listitem>

<listitem><para>Start X by typing <computeroutput>startx</computeroutput> 


</para></listitem>

<listitem><para>Open a terminal window type and login as root 

<programlisting>
$ su -
; Enter the <phrase>root</phrase> password when prompted.
#
       
</programlisting>
</para></listitem>

<listitem><para>Create and setup the <phrase>oracle</phrase> group and
<phrase>oracle</phrase> account </para>

<para>We need to create a user <phrase>oracle</phrase>, which is used to install
the product, as well as starting and stopping the database.</para>

<programlisting>
# groupadd dba
# groupadd oinstall
# groupadd oracle
# useradd -g dba -G oinstall,oracle -m oracle
   
</programlisting>

<para>
Now change the <phrase>oracle</phrase> account password
</para>


<programlisting>
# passwd oracle
   
</programlisting>

<para>You will be prompted for the New Password and Confirmation of that
password.</para>
</listitem>

<listitem><para>Setup the installation location for Oracle </para>

<para>While Oracle can reside in a variety of places in the file system,
ArsDigita has adopted &#39;/ora8&#39; as the base directory.</para>

<para><phrase>Note:</phrase> the Oracle install needs about 1 GB free on
&#39;/ora8&#39; to install successfully.</para>

<programlisting>
# mkdir /ora8
# cd /ora8
# mkdir -p m01 m02 m03/oradata/ora8
# chown -R oracle.dba /ora8
# exit                  ; Logs out.
   
</programlisting>
</listitem>

<listitem><para>Set up the <emphasis>oracle</emphasis> user environment</para>



<itemizedlist>
<listitem><para>Log in as the user <phrase>oracle</phrase> </para>

<para>In the same terminal window, type the following.</para>

<programlisting>
$ su - oracle
; Enter oracle&#39;s password
       
</programlisting>
</listitem>

<listitem><para>Use a text editor to edit the <phrase>.bash_profile</phrase> file in the
<phrase>oracle</phrase> account home directory. </para>

<programlisting>
$ emacs ~oracle/.bash_profile
       
</programlisting>
<para>
You may get this error trying to start emacs: 
</para>
<programlisting>
Xlib: connection to &quot;:0.0&quot; refused by server
Xlib: Client is not authorized to connect to Server
emacs: Cannot connect to X server :0.
Check the DISPLAY environment variable or use `-d&#39;.
Also use the `xhost&#39; program to verify that it is set to permit
connections from your machine.
       
</programlisting>

<para>
If so, do the following. 
</para>

<programlisting>
Open a new terminal window.
$ xhost +localhost
Now, back in the oracle terminal
$ export DISPLAY=localhost:0.0
$ emacs ~oracle/.bash_profile
       
</programlisting>
<para>
Try this procedure anytime you get an Xlib connection refused error. 


</para></listitem>

<listitem><para>Add the following lines to ~oracle/.bash_profile:</para>



<programlisting>
export ORACLE_BASE=/ora8/m01/app/oracle
export ORACLE_HOME=$ORACLE_BASE/product/8.1.6
export PATH=$PATH:$ORACLE_HOME/bin
export LD_LIBRARY_PATH=$ORACLE_HOME/lib:/lib:/usr/lib
export ORACLE_SID=ora8
export ORACLE_TERM=vt100
export ORA_NLS33=$ORACLE_HOME/ocommon/nls/admin/data

umask 022
       
</programlisting>

<para>Save the file by typing <computeroutput>CTRL-X CTRL-S</computeroutput> and then exit by typing
<computeroutput>CTRL-X CTRL-C</computeroutput>. Alternatively, use the menus.</para>
</listitem>
</itemizedlist>

<para>
Make sure that you do <phrase>not</phrase> add any lines like the following 
</para>

<programlisting>
# NLS_LANG=american
# export NLS_LANG
   
</programlisting>

<para>These lines will change the Oracle date settings and will break the
ArsDigita Community System (ACS) because ACS depends on the ANSI date format,
YYYY-MM-DD dates.</para>
</listitem>

<listitem><para>Log out as oracle

<programlisting>
$ exit
</programlisting>
</para></listitem>

<listitem><para>Log back in as oracle and double check that your environment variables
are as intended 
</para>


<programlisting>
$ su - oracle
$ env | grep ORA
   
</programlisting>

<para>
If it worked, you should see: 
</para>
<programlisting>
ORACLE_SID=ora8
ORACLE_BASE=/ora8/m01/app/oracle
ORACLE_TERM=vt100
ORACLE_HOME=/ora8/m01/app/oracle/product/8.1.6
ORA_NLS33=$ORACLE_HOME/ocommon/nls/admin/data   
   
</programlisting>

<para>If not, try adding the files to <computeroutput>~oracle/.bashrc</computeroutput> instead of
<computeroutput>.bash_profile</computeroutput>. Then logout and log back in again. Also, be
certain you are doing <computeroutput>su -</computeroutput> and not just <computeroutput>su</computeroutput>. The
<computeroutput>-</computeroutput> means that <computeroutput>.bashrc</computeroutput> and <computeroutput>.bash_profile</computeroutput>
will be evaluated.</para>

<para>Make sure that <phrase>/bin</phrase>, <phrase>/usr/bin</phrase>, and
<phrase>/usr/local/bin</phrase> are in your path by typing:</para>

<programlisting>
$ echo $PATH
/bin:/usr/bin:/usr/local/bin:/usr/bin/X11:/usr/X11R6/bin:/home/oracle/bin:/ora8/m01/app/oracle/product/8.1.6/bin
   
</programlisting>

<para>If they are not, then add them to the .bash_profile by changing the PATH
statement above to
<computeroutput>PATH=$PATH:/usr/local/bin:$ORACLE_HOME/bin</computeroutput></para>
</listitem>
</itemizedlist>


</sect2>

<sect2 id="install-oracle-install">
<title>Installing Oracle 8.1.6 Server</title>


<itemizedlist>
<listitem><para>Log in as the user <phrase>oracle</phrase> and start X if not already
running
</para>


<programlisting>
Start a new terminal
$ xhost +localhost
$ su - oracle
Enter oracle password
$ export DISPLAY=localhost:0.0
   
</programlisting>
</listitem>

<listitem><para>Find the &#39;runInstaller&#39; script</para>



<itemizedlist>
<listitem><para>If you are installing Oracle from a CD-ROM, it is located in the
&#39;install/linux&#39; path from the cd-rom mount point
</para>


<programlisting>
$ su - root
# mount -t iso9660 /dev/cdrom /mnt/cdrom
# exit
$ cd /mnt/cdrom
       
</programlisting>
</listitem>

<listitem><para>If you are installing from the tarball, the install script is located in
the &#39;Oracle8iR2&#39; directory that was created when you expanded the
archive.
</para>


<programlisting>
$ cd /where/oracle/archive/is/Oracle8iR2
       
</programlisting>
</listitem>
</itemizedlist>
<para>
Check to make sure the file is there. 
</para>
<programlisting>
$ ls
doc  index.htm  install  runInstaller  stage  starterdb
   
</programlisting>
<para>
If you don&#39;t see <computeroutput>runInstaller</computeroutput> you are in the wrong
directory. 

</para>
</listitem>

<listitem><para>Run the installer
</para>


<programlisting>
$ ./runInstaller
   
</programlisting>

<itemizedlist>
<listitem><para>A window will open that welcomes you to the &#39;Oracle Universal
Installer&#39; (OUI). Click on &quot;<phrase>Next</phrase>&quot;


</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;File Locations&quot; screen in the OUI: 
</para>


<itemizedlist>
<listitem><para>&quot;Source&quot; path should have been prefilled with &quot;(wherever
you mounted the CDROM)/stage/products.jar&quot;


</para></listitem>

<listitem><para>&quot;destination&quot; path says
&quot;/ora8/m01/app/oracle/product/8.1.6&quot; </para>

<para>If the destination is not correct it is because your environment variables
are not set properly. Make sure you logged on as <phrase>oracle</phrase>
using <computeroutput>su - oracle</computeroutput>. If so, edit the
<computeroutput>~oracle/.bash_profile</computeroutput> as you did in <xref linkend="install-oracle-preinstall"/></para>

</listitem>

<listitem><para>Click &quot;Next&quot; (a pop up window will display Loading Product
information).


</para></listitem>
</itemizedlist>

</listitem>

<listitem><para>The &quot;Unix Group Name&quot; screen in the OUI:

</para>

<itemizedlist>
<listitem><para>The Unix Group name needs to be set to &#39;oinstall&#39; ( we made this
Unix group earlier ).


</para></listitem>

<listitem><para>Click &quot;Next&quot;


</para></listitem>

<listitem><para>A popup window appears instantly, requesting you to run a script a root:


</para></listitem>

<listitem><para>Open a new terminal window, then type:

</para>

<programlisting>
$ su
# cd /ora8/m01/app/oracle/product/8.1.6
# ./orainstRoot.sh  
; You should see:
Creating Oracle Inventory pointer file (/etc/oraInst.loc)
Changing groupname of /ora8/m01/app/oracle/oraInventory to oinstall.      
# exit
$ exit
     
</programlisting>
</listitem>

<listitem><para>Click &quot;Retry&quot;

</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Available Products&quot; screen in the OUI:
</para>


<itemizedlist>
<listitem><para>Select &quot;Oracle 8i Enterprise Edition 8.1.6.1.0&quot;


</para></listitem>

<listitem><para>Click &quot;Next&quot;


</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Installation Types&quot; screen
</para>


<itemizedlist>
<listitem><para>Select the &quot;Custom&quot; installation type.


</para></listitem>

<listitem><para>Click &quot;Next&quot;


</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Available Product Components&quot; screen
</para>


<itemizedlist>
<listitem><para>In addition to the defaults, make sure that &quot;Oracle SQLJ
8.1.6.0,&quot; &quot;Oracle Protocol Support 8.1.6.1.0,&quot; and &quot;Linux
Documentation 8.1.6.0.0&quot; are also checked (they were not in release
8.1.5).


</para></listitem>

<listitem><para>Click &quot;Next&quot;


</para></listitem>

<listitem><para>A progress bar will appear for about 1 minute.


</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Component Locations&quot; screen in the OUI
</para>


<itemizedlist>
<listitem><para>Click on the &quot;Java Runtime Environment 1.1.8&quot; It should have
the path &quot;/ora8/m01/app/oracle/jre/1.1.8&quot;


</para></listitem>

<listitem><para>Click &quot;Next&quot;


</para></listitem>

<listitem><para>A progress bar will appear for about 1 minute.


</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Privileged Operation System Groups&quot; screen in the OUI
</para>


<itemizedlist>
<listitem><para>Enter &quot;dba&quot; for &quot;Database Administrator (OSDBA)
Group&quot;


</para></listitem>

<listitem><para>Enter &quot;dba&quot; for the &quot;Database Operator (OSOPER)
Group&quot;


</para></listitem>

<listitem><para>Click &quot;Next&quot;


</para></listitem>

<listitem><para>A progress bar will appear for about 1 minute.

</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Create a Database&quot; screen in the OUI

</para>

<itemizedlist>
<listitem><para>Select &quot;No&quot; as we will do this later, after some important
configuration changes.


</para></listitem>

<listitem><para>Click &quot;Next&quot;


</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Summary&quot; screen in the OUI

</para>

<itemizedlist>
<listitem><para>Check the &quot;Space Requirements&quot; section to verify you have
enough disk space for the install.


</para></listitem>

<listitem><para>Check that &quot;(91 products)&quot; is in the &quot;New
Installations&quot; section title.


</para></listitem>

<listitem><para>Click &quot;Install&quot;


</para></listitem>

<listitem><para>A progress bar will appear for about 20- 30 minutes. Now is a good time
to take a break.


</para></listitem>

<listitem><para>A &quot;Setup Privileges&quot; window will popup towards the end of the
installation asking you to run a script as <emphasis>root</emphasis>

</para></listitem>

<listitem><para>Run the script.



<programlisting>
$ su -
Enter root password
# /ora8/m01/app/oracle/product/8.1.6/root.sh
; You should see the following.   

Creating Oracle Inventory pointer file (/etc/oraInst.loc)
Changing groupname of /ora8/m01/app/oracle/oraInventory to oinstall.
# /ora8/m01/app/oracle/product/8.1.6/root.sh
Running Oracle8 root.sh script...
The following environment variables are set as:
    ORACLE_OWNER= oracle
    ORACLE_HOME=  /ora8/m01/app/oracle/product/8.1.6
    ORACLE_SID=   ora8

Enter the full pathname of the local bin directory: [/usr/local/bin]: 

<phrase>Press ENTER here to accept default of /usr/local/bin</phrase>
      

Creating /etc/oratab file...
Entry will be added to the /etc/oratab file by
Database Configuration Assistant when a database is created
Finished running generic part of root.sh script.
Now product-specific root actions will be performed.
IMPORTANT NOTE: Please delete any log and trace files previously
                created by the Oracle Enterprise Manager Intelligent
                Agent. These files may be found in the directories
                you use for storing other Net8 log and trace files.
                If such files exist, the OEM IA may not restart.

     
</programlisting>
</para></listitem>

<listitem><para>Do not follow the instructions on deleting trace and log files, it is not
necessary.


</para></listitem>
</itemizedlist>

<programlisting>
# exit
$ exit
</programlisting>
</listitem>

<listitem><para>Go back to the pop-up window and click &quot;OK&quot;


</para></listitem>

<listitem><para>The &quot;Configuration Tools&quot; screen in the OUI

</para>

<itemizedlist>
<listitem><para>This window displays the config tools that will automatically be
launched.

</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Welcome&quot; screen in the &quot;net 8 Configuration
Assistant&quot;

</para>

<itemizedlist>
<listitem><para>Make sure the &quot;Perform Typical installation&quot; is
<phrase>not</phrase> selected.


</para></listitem>

<listitem><para>Click &quot;Next&quot;


</para></listitem>
</itemizedlist>

<itemizedlist>
<listitem><para>The &quot;Directory Service Access&quot; screen in the &quot;Net 8
Configuration Assistant&quot;


</para></listitem>

<listitem><para>Select &quot;No&quot;


</para></listitem>

<listitem><para>Click &quot;Next&quot;


</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Listener Configuration, Listener Name&quot; screen in the
&quot;Net 8 Configuration Assistant&quot;

</para>

<itemizedlist>
<listitem><para>Accept the default listener name of &quot;LISTENER&quot;


</para></listitem>

<listitem><para>Click &quot;Next&quot;


</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Listener Configuration, Select Protocols&quot; screen in the
&quot;Net 8 Configuration Assistant&quot;

</para>

<itemizedlist>
<listitem><para>The only choice in &quot;Select protocols:&quot; should be
&quot;TCP/IP&quot;


</para></listitem>

<listitem><para>Click &quot;Next&quot;

</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Listener Configuration TCP/IP Protocol&quot; screen in the
&quot;Net 8 Configuration Assistant&quot;

</para>

<itemizedlist>
<listitem><para>Default Port should be 1521 and selected.


</para></listitem>

<listitem><para>Click &quot;Next&quot;


</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Listener Configuration, More Listeners&quot; screen in the
&quot;Net 8 Configuration Assistant&quot;
</para>


<itemizedlist>
<listitem><para>Select &quot;No&quot;


</para></listitem>

<listitem><para>Click &quot;Next&quot;

</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Listener Configuration Done&quot; screen in the &quot;Net 8
Configuration Assistant&quot;
</para>


<itemizedlist>
<listitem><para>Click &quot;Next&quot;

</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Naming Methods Configuration&quot; screen in the &quot;Net 8
Configuration Assistant&quot;

</para>

<itemizedlist>
<listitem><para>Select &quot;No&quot;


</para></listitem>

<listitem><para>Click &quot;Next&quot;

</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Done&quot; screen in the &quot;Net 8 Configuration
Assistant&quot;
</para>


<itemizedlist>
<listitem><para>Click &quot;Finish&quot;


</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;End of Installation&quot; screen in the OUI

</para>

<itemizedlist>
<listitem><para>Click &quot;Exit&quot;


</para></listitem>

<listitem><para>Click &quot;Yes&quot; on the confirmation pop up window.


</para></listitem>

<listitem><para>The Oracle Universal Installer window should have disappeared!

</para></listitem>
</itemizedlist>
</listitem>
</itemizedlist>

<para>Congratulations, you have just installed Oracle 8.1.6 Server! However, you
still need to create a database which can take about an hour of
non-interactive time, so don&#39;t quit yet.</para>


</sect2>

<sect2 id="install-oracle-create">
<title>Creating the First Database</title>


<para>This step will take you through the steps of creating a customized
database. Be warned that this process takes about an hour on a Pentium II
with 128 MB of RAM.</para>

<itemizedlist>
<listitem><para>Make sure you are running X. Open up a terminal and su to oracle and then
run the <computeroutput>dbassist</computeroutput> program. 
</para>

<programlisting>
$ xhost +localhost
$ su - oracle
; Enter oracle password
$ export DISPLAY=localhost:0.0
$ dbassist
 
</programlisting>


</listitem>

<listitem><para>The &quot;Welcome&quot; screen in the Oracle Database Configuration Agent
(ODCA) 
</para>


<itemizedlist>
<listitem><para>Select &quot;Create a database&quot;


</para></listitem>

<listitem><para>Click &quot;Next&quot;
</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Select database type&quot; screen in the ODCA 

</para>

<itemizedlist>
<listitem><para>Select &quot;Custom&quot;


</para></listitem>

<listitem><para>Click &quot;Next&quot;

</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;Primary Database Type&quot; window in ODCA 
</para>


<itemizedlist>
<listitem><para>Select &quot;Multipurpose&quot;


</para></listitem>

<listitem><para>Click &quot;Next&quot;

</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>The &quot;concurrent users&quot; screen of the ODCA 
</para>


<itemizedlist>
<listitem><para>Select &quot;60&quot; concurrent users. 


</para></listitem>

<listitem><para>Click &quot;Next&quot;</para></listitem>
</itemizedlist>


</listitem>

<listitem><para>Select &quot;<phrase>Dedicated Server Mode</phrase>&quot;, click
&quot;<phrase>Next</phrase>&quot; 


</para></listitem>

<listitem><para>Accept all of the options, and click &quot;<phrase>Next</phrase>&quot;
Oracle Visual Information Retrieval may be grayed out. If so, you can ignore
it; just make sure that everything else is checked. 


</para></listitem>

<listitem><para>For &quot;Global Database Name&quot;, enter
&quot;<phrase>ora8</phrase>&quot;; for &quot;SID&quot;, also enter
&quot;<phrase>ora8</phrase>&quot; (it should do this automatically). Click
&quot;<phrase>Next</phrase>&quot;.


</para></listitem>

<listitem><para>Accept the defaults for the next screen (control file location). Click
&quot;<phrase>Next</phrase>&quot;


</para></listitem>

<listitem><para>Go to the &quot;<phrase>temporary</phrase>&quot; and
&quot;<phrase>rollback</phrase>&quot; tabs, and change the Size (upper-right
text box) to <phrase>150</phrase>MB. Click &quot;<phrase>Next</phrase>&quot;


</para></listitem>

<listitem><para>Increase the redo log sizes to <phrase>10000K</phrase> each. Click
&quot;<phrase>Next</phrase>&quot;


</para></listitem>

<listitem><para>Use the default checkpoint interval &amp; timeout. Click
&quot;<phrase>Next</phrase>&quot;


</para></listitem>

<listitem><para>Increase &quot;<phrase>Processes</phrase>&quot; to <phrase>100</phrase>;
&quot;<phrase>Block Size</phrase>&quot; to <phrase>4096</phrase> (better for
small Linux boxes; aD uses 8192 on the big Solaris machines).


</para></listitem>

<listitem><para>Accept the defaults for the Trace File Directory. Click
&quot;<phrase>Next</phrase>&quot;


</para></listitem>

<listitem><para>Finally, select &quot;<phrase>Save information to a shell
script</phrase>&quot; and click &quot;<phrase>Finish</phrase>&quot;
(We&#39;re going to examine the contents of this file before creating our
database.)


</para></listitem>

<listitem><para>Click the &quot;<phrase>Save</phrase>&quot; button. Oracle will
automatically save it to the correct directory and with the correct file
name. This will likely be
<computeroutput>/ora8/m01/app/oracle/product/8.1.6/assistants/dbca/jlib/sqlora8.sh</computeroutput>



</para></listitem>

<listitem><para>It will alert you that the script has been saved successfully.


</para></listitem>

<listitem><para>Now we need to customize the database configuration a bit. While still
logged on as <phrase>oracle</phrase>, edit the database initialization script
(run when the db loads). The scripts are kept in
<computeroutput>$ORACLE_HOME/dbs</computeroutput> and the name of the script is usually
<computeroutput>init</computeroutput><emphasis>SID</emphasis><computeroutput>.ora</computeroutput> where <emphasis>SID</emphasis> is the SID of your
database. Assuming your $ORACLE_HOME matches our default of
<computeroutput>/ora8/m01/app/oracle/product/8.1.6</computeroutput>, the following will open the
file for editing. 
</para>
<programlisting>
$ emacs /ora8/m01/app/oracle/product/8.1.6/dbs/initora8.ora  
</programlisting>
</listitem>

<listitem><para>Add the following line to the end:

</para>

<programlisting>
nls_date_format = &quot;YYYY-MM-DD&quot;
</programlisting>
</listitem>

<listitem><para>Now find the <computeroutput>open_cursors</computeroutput> line in the file. If you&#39;re
using <computeroutput>emacs</computeroutput> scroll up to the top of the buffer and do
<computeroutput>CTRL-S</computeroutput> and type <computeroutput>open_cursors</computeroutput> to find the line. The
default is <phrase>100</phrase>. Change it to <phrase>500</phrase>. 
</para>
<programlisting>
open_cursors = 500
</programlisting>
</listitem>

<listitem><para>Save the file. In emacs, do <computeroutput>CTRL-X CTRL-S</computeroutput> to save followed
by <computeroutput>CTRL-X CTRL-C</computeroutput> to exit or use the menu.


</para></listitem>

<listitem><para>At this point, you are ready to initiate database creation. We recommend
shutting down X to free up some RAM unless you have 256 MB of RAM or more.
You can do this quickly by doing a <computeroutput>CRTL-ALT-BACKSPACE</computeroutput>, but make
sure you have saved any files you were editing. You should now be returned to
a text shell prompt. If you get sent to a graphical login screen instead,
switch to a virtual console by doing <computeroutput>CRTL-ALT-F1</computeroutput>. Then login as
<phrase>oracle</phrase>. 


</para></listitem>

<listitem><para>Change to the directory where the database creation script is and run it:


<programlisting>
$ cd /ora8/m01/app/oracle/product/8.1.6/assistants/dbca/jlib
$ ./sqlora8.sh
     
</programlisting>

In some instances, Oracle will save the file to
<computeroutput>/ora8/m01/app/oracle/product/8.1.6/assistants/dbca</computeroutput> Try running
the script there if your first attempt does not succeed.


</para></listitem>

<listitem><para>Your database will now be built. It will take &gt; 1 hour - no fooling.
You will see lots of errors scroll by (like: &quot;ORA-01432: public synonym
to be dropped does not exist&quot;) Fear not, this is normal.

</para>

<para>Eventually, you&#39;ll be returned to your shell prompt. In the meantime,
relax, you&#39;ve earned it.</para>


</listitem>
</itemizedlist>



</sect2>

<sect2 id="istall-oracle-test">
<title>Acceptance Test</title>


<para>For this step, open up a terminal and su to oracle as usual. You should be
running X and Netscape for this phase.</para>

<itemizedlist>
<listitem><para>You need to download the &quot;Oracle Acceptance Test&quot; file.
It&#39;s available <ulink url="files/acceptance-sql.txt">here</ulink> and at <ulink url="http://philip.greenspun.com/wtr/oracle/acceptance-sql.txt">http://photo.net/wtr/oracle/acceptance-sql.txt</ulink>.
Save the file to <computeroutput>/tmp</computeroutput>

</para></listitem>

<listitem><para>In the oracle shell, copy the file.

</para>

<programlisting>
$ cp /tmp/acceptance-sql.txt /tmp/acceptance.sql
 
</programlisting>
</listitem>

<listitem><para>Once you&#39;ve got the acceptance test file all set, stay in your term
and type the following: 

</para>

<programlisting>
$ sqlplus system/manager
 
</programlisting>

<para>SQL*Plus should startup. If you get an <computeroutput>ORA-01034: Oracle not
Available</computeroutput> error, it is because your Oracle instance is not running.
You can manually start it as the <computeroutput>oracle</computeroutput> user.</para>


<programlisting>
$ svrmgrl
SVRMGR&gt; connect internal
SVRMGR&gt; startup
 
</programlisting>
</listitem>

<listitem><para>Now that you&#39;re into SQL*Plus, change the default passwords for
system, sys, and ctxsys to &quot;alexisahunk&quot; (or to something
you&#39;ll remember): 
</para>
<programlisting>
SQL&gt; alter user system identified by alexisahunk;
SQL&gt; alter user sys identified by alexisahunk;
SQL&gt; alter user ctxsys identified by alexisahunk;
</programlisting>
</listitem>

<listitem><para>Verify that your date settings are correct. 
</para>
<programlisting>
SQL&gt; select sysdate from dual;
 
</programlisting>
<para>
If you don&#39;t see a date that fits the format <computeroutput>YYYY-MM-DD</computeroutput>,
please read <xref linkend="install-oracle-troubleshooting"/>.


</para></listitem>

<listitem><para>At this point we are going to hammer your database with an intense
acceptance test. This usually takes around 30 minutes. 
</para>
<programlisting>
SQL&gt; @ /tmp/acceptance.sql

; A bunch of lines will scroll by.  You&#39;ll know if the test worked if
; you see this at the end:

SYSDATE
----------
2000-06-10

SQL&gt; 
 
</programlisting>

<para>Many people encounter an error regarding <computeroutput>maximum key
length</computeroutput>:</para>

<programlisting>
ERROR at line 1:
ORA-01450: maximum key length (758) exceeded
     
</programlisting>

<para>This error occurs if your database block size is wrong and is usually
suffered by people trying to load the ACS into a pre-existing database.
Unfortunately, the only solution is to create a new database with a block
size of at least <phrase>4096</phrase>. For instructions on how to do this,
see <xref linkend="install-oracle-create"/> above. You can set the
parameter using the <computeroutput>dbassist</computeroutput> program or by setting the
<computeroutput>DB_BLOCK_SIZE</computeroutput> parameter in your database&#39;s creation
script.</para>

<para>If there were no errors, then consider yourself fortunate. Your Oracle
installation is working.</para>


</listitem>
</itemizedlist>


</sect2>

<sect2 id="install-oracle-automating">
<title>Automating Startup &amp; Shutdown</title>


<para>You will want to automate the database startup and shutdown process.
It&#39;s probably best to have Oracle spring to life when you boot up your
machine.</para>

<itemizedlist>
<listitem><para>Oracle includes a script called <phrase>dbstart</phrase> that can be used
to automatically start the database. Unfortunately, the script shipped in the
Linux distribution does not work out of the box. The fix is simple. Follow
these directions to apply it. First, save <ulink url="files/dbstart.txt">dbstart</ulink>
to <phrase>/tmp</phrase>. Then login, and su to <phrase>oracle</phrase>. </para>

<programlisting>
$ cp /tmp/dbstart.txt /ora8/m01/app/oracle/product/8.1.6/bin/dbstart 
$ chmod 755 /ora8/m01/app/oracle/product/8.1.6/bin/dbstart 
 
</programlisting>
</listitem>

<listitem><para>While you&#39;re logged in as <phrase>oracle</phrase>, you should
configure the <phrase>oratab</phrase> file to load your database at start.
Edit the file <phrase>/etc/oratab</phrase>: </para>



<itemizedlist>
<listitem><para>You will see this line. </para>

<programlisting>
ora8:/ora8/m01/app/oracle/product/8.1.6:N
</programlisting>

<para>By the way, if you changed the service name or have multiple databases,
the format of this file is</para>

<para><emphasis><computeroutput>service_name:$ORACLE_HOME:Y || N (for
autoload)</computeroutput></emphasis></para>
</listitem>

<listitem><para>Change the last letter from &quot;N&quot; to &quot;Y&quot;. This tells
Oracle that you want the database to start when the machine boots. It should
look like this.
</para>


<programlisting>
ora8:/ora8/m01/app/oracle/product/8.1.6:Y
   
</programlisting>


</listitem>

<listitem><para>Save the file &amp; quit the terminal.


</para></listitem>
</itemizedlist>
</listitem>

<listitem><para>You need a script to automate startup and shutdown. Save <ulink url="files/oracle8i.txt">oracle8i.txt</ulink> in /tmp. Then login as
<phrase>root</phrase> and install the script. </para>

<programlisting>
 
$ su -
# cp /tmp/oracle8i.txt /etc/rc.d/init.d/oracle8i
# chown root.root /etc/rc.d/init.d/oracle8i
# chmod 700 /etc/rc.d/init.d/oracle8i      
 
</programlisting>
</listitem>

<listitem><para>Test the script by typing the following commands and checking the output.</para>


<programlisting>
# /etc/rc.d/init.d/oracle8i stop
Oracle 8i auto start/stop
Shutting Oracle8i:
Oracle Server Manager Release 3.1.6.0.0 - Production

Copyright (c) 1997, 1999, Oracle Corporation.  All Rights Reserved.

Oracle8i Enterprise Edition Release 8.1.6.1.0 - Production
With the Partitioning option
JServer Release 8.1.6.0.0 - Production

SVRMGR&gt; Connected.
SVRMGR&gt; Database closed.
Database dismounted.
ORACLE instance shut down.
SVRMGR&gt; 
Server Manager complete.
Database &quot;ora8&quot; shut down.
      
# /etc/rc.d/init.d/oracle8i start
Oracle 8i auto start/stop
Starting Oracle8i: 
SQL*Plus: Release 8.1.6.0.0 - Production on Sat Jun 10 17:56:02 2000

(c) Copyright 1999 Oracle Corporation.  All rights reserved.

SQL&gt; Connected to an idle instance.
SQL&gt; ORACLE instance started.

Total System Global Area   85004272 bytes
Fixed Size            69616 bytes
Variable Size          76374016 bytes
Database Buffers        8388608 bytes
Redo Buffers             172032 bytes
Database mounted.
Database opened.
SQL&gt; Disconnected

Database &quot;ora8&quot; warm started.
 
</programlisting>
</listitem>

<listitem><para>If it worked, then run these commands to make the startup and shutdown
automatic. </para>

<programlisting>
# cd /etc/rc.d/init.d/                      
# chkconfig --add oracle8i
# chkconfig --list oracle8i
; You should see:
oracle8i        0:off   1:off   2:off   3:on    4:on    5:on    6:off
 
</programlisting>
</listitem>

<listitem><para>You also need some scripts to automate startup and shutdown of the
Oracle8i listener. The listener is a name server that allows your Oracle
programs to talk to local and remote databases using a standard naming
convention. It is required for Intermedia Text and full site search.</para>

<para>Download these three scripts into <phrase>/tmp</phrase></para>

<itemizedlist>
<listitem><para><ulink url="files/startlsnr.txt ">startlsnr.txt</ulink></para></listitem>

<listitem><para><ulink url="files/stoplsnr.txt ">stoplsnr.txt</ulink></para></listitem>

<listitem><para><ulink url="files/listener8i.txt">listener8i.txt</ulink></para></listitem>
</itemizedlist>

<para>Now issue the following commands (still as <phrase>root</phrase>).</para>

<programlisting>
# su - oracle
# cp /tmp/startlsnr.txt /ora8/m01/app/oracle/product/8.1.6/bin/startlsnr
$ cp /tmp/stoplsnr.txt /ora8/m01/app/oracle/product/8.1.6/bin/stoplsnr    
$ chmod 700 /ora8/m01/app/oracle/product/8.1.6/bin/startlsnr
$ chmod 700 /ora8/m01/app/oracle/product/8.1.6/bin/stoplsnr
$ exit
; You should now be back as root.
# cp /tmp/listener8i.txt /etc/rc.d/init.d/listener8i
# cd /etc/rc.d/init.d
# chmod 700 listener8i
  
</programlisting>

<para>Test the listener automation by running the following commands and
checking the output.</para>

<programlisting>
# ./listener8i stop
Oracle 8i listener start/stop
Shutting down Listener for 8i: 
LSNRCTL for Linux: Version 8.1.6.0.0 - Production on 10-JUN-2000 18:28:49

(c) Copyright 1998, 1999, Oracle Corporation.  All rights reserved.

Connecting to (DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=localhost.localdomain)(PORT=1521)))
The command completed successfully

    
# ./listener8i start
Oracle 8i listener start/stop
Starting the Listener for 8i: 
LSNRCTL for Linux: Version 8.1.6.0.0 - Production on 10-JUN-2000 18:28:52

(c) Copyright 1998, 1999, Oracle Corporation.  All rights reserved.

Starting /ora8/m01/app/oracle/product/8.1.6/bin/tnslsnr: please wait...

TNSLSNR for Linux: Version 8.1.6.0.0 - Production
System parameter file is /ora8/m01/app/oracle/product/8.1.6/network/admin/listener.ora
Log messages written to /ora8/m01/app/oracle/product/8.1.6/network/log/listener.log
Listening on: (DESCRIPTION=(ADDRESS=(PROTOCOL=tcp)(HOST=localhost.localdomain)(PORT=1521)))
Listening on: (DESCRIPTION=(ADDRESS=(PROTOCOL=ipc)(KEY=EXTPROC)))

Connecting to (DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=localhost.localdomain)(PORT=1521)))
STATUS of the LISTENER
------------------------
Alias                     LISTENER
Version                   TNSLSNR for Linux: Version 8.1.6.0.0 - Production
Start Date                10-JUN-2000 18:28:53
Uptime                    0 days 0 hr. 0 min. 0 sec
Trace Level               off
Security                  OFF
SNMP                      OFF
Listener Parameter File   /ora8/m01/app/oracle/product/8.1.6/network/admin/listener.ora
Listener Log File         /ora8/m01/app/oracle/product/8.1.6/network/log/listener.log
Services Summary...
  PLSExtProc        has 1 service handler(s)
  ora8      has 1 service handler(s)
The command completed successfully
   
</programlisting>

<para>This test will verify that the listener is operating normally. Login into
the database using the listener naming convention.</para>

<para><computeroutput>sqlplus</computeroutput> <emphasis><computeroutput>username/password/@SID</computeroutput></emphasis></para>



<programlisting>
# su - oracle
$ sqlplus system/alexisahunk@ora8

SQL&gt; select sysdate from dual;

SYSDATE
----------
2000-06-10

SQL&gt; exit
$ exit
#
   
</programlisting>

<para>Now run <computeroutput>chkconfig</computeroutput> on the <computeroutput>listener8i</computeroutput> script.</para>

<programlisting>
# cd /etc/rc.d/init.d/
# chkconfig --add listener8i
# chkconfig --list listener8i
listener8i      0:off   1:off   2:off   3:on    4:on    5:on    6:off
   
</programlisting>


</listitem>

<listitem><para>Test the automation</para>

<para>As a final test, reboot your computer and make sure Oracle comes up. You
can do this by typing</para>

<programlisting>
# /sbin/shutdown -r -t 0 now
   
</programlisting>

<para>
Log back in and ensure that Oracle started automatically. 
</para>

<programlisting>
$ su - oracle
$ sqlplus system/alexisahunk@ora8

SQL&gt; exit
  
</programlisting>
</listitem>
</itemizedlist>

<para>Congratulations, your installation of Oracle 8.1.6 is complete.</para>



</sect2>

<sect2 id="install-oracle-troubleshooting">
<title>Troubleshooting Oracle Dates</title>


<para>Oracle has an internal representation for storing the data based on the
number of seconds elapsed since some date. However, for the purposes of
inputing dates into Oracle and getting them back out, Oracle needs to be told
to use a specific date format. By default, it uses an Oracle-specific format
which isn&#39;t copacetic. You want Oracle to use the ANSI-compliant date
format which is of form <phrase>&#39;YYYY-MM-DD&#39;</phrase>.</para>

<para>To fix this, you should include the following line in
<phrase><computeroutput>$ORACLE_HOME/dbs/init</computeroutput><emphasis>SID</emphasis><computeroutput>.ora</computeroutput></phrase> or for
the default case,
<phrase><computeroutput>$ORACLE_HOME/dbs/initora8.ora</computeroutput></phrase></para>

<programlisting>
nls_date_format = &quot;YYYY-MM-DD&quot;
</programlisting>

<para>
You test whether this solved the problem by firing up
<phrase>sqlplus</phrase> and typing: 
</para>

<programlisting>
SQL&gt; select sysdate from dual;
</programlisting>

<para>You should see back a date like <computeroutput>2000-06-02</computeroutput>. If some of the
date is chopped off, i.e. like <computeroutput>2000-06-0</computeroutput>, everything is still
fine. The problem here is that <phrase>sqlplus</phrase> is simply truncating
the output. You can fix this by typing:</para>

<programlisting>
SQL&gt; column sysdate format a15
SQL&gt; select sysdate from dual;
</programlisting>

<para>If the date does not conform to this format, double-check that you
included the necessary line in the init scripts. If it still isn&#39;t
working, make sure that you have restarted the database since adding the line
if you didn&#39;t do it prior to database creation.</para>

<para>If you&#39;re sure that you have restarted the database since adding the
line, check your initialization scripts. Make sure that the following line is
not included:</para>

<programlisting>
export nls_lang = american
</programlisting>

<para>Setting this environment variable will override the date setting. Either
delete this line and login again or add the following entry to your login
scripts <emphasis>after</emphasis> the <computeroutput>nls_lang</computeroutput> line:</para>

<programlisting>
export nls_date_format = &#39;YYYY-MM-DD&#39;
</programlisting>

<para>Log back in again. If adding the <computeroutput>nls_date_format</computeroutput> line
doesn&#39;t help, you can ask for advice in our
<ulink url="http://www.arsdigita.com/bboard/q-and-a?topic_id=21&amp;topic=web%2fdb">web/db forum</ulink>.</para>


</sect2>

<sect2 id="install-oracle-procs">
<title>Useful Procedures</title>


<itemizedlist>
<listitem><para>Dropping a tablespace</para>



<itemizedlist>
<listitem><para>Run sqlplus as the dba: </para>

<programlisting>
$ sqlplus system/changeme
</programlisting>
</listitem>

<listitem><para>To drop a user and all of the tables and data owned by that user: </para>

<programlisting>
SQL&gt; drop user <emphasis>oracle_user_name</emphasis> cascade;
</programlisting>
</listitem>

<listitem><para>To drop the tablespace: This will delete everything in the tablespace
overriding any referential integrity constraints. Run this command only if
you want to clean out your database entirely. 
</para>
<programlisting>
SQL&gt; drop tablespace <emphasis>table_space_name</emphasis> including contents cascade constraints;
</programlisting>
</listitem>
</itemizedlist>
</listitem>
</itemizedlist>

<para>For more information on Oracle, please consult the <ulink url="http://oradoc.photo.net/">documentation</ulink>.</para>



</sect2>

<sect2 id="install-oracle-defaults">
<title>Defaults</title>


<para>We used the following defaults while installing Oracle.</para>

 
<informaltable>
<tgroup cols="3">
<tbody>
<row>
<entry><phrase>Variable</phrase></entry>
<entry><phrase>Value</phrase></entry>
<entry><phrase>Reason</phrase></entry>
</row>

<row>
<entry>ORACLE_HOME</entry>
<entry>/ora8/m01/app/oracle/product/8.1.6</entry>
<entry>This is the default Oracle installation directory.</entry>
</row>

<row>
<entry>ORACLE_SERVICE</entry>
<entry>ora8</entry>
<entry>The service name is a domain-qualified identifier for your Oracle
server.</entry>
</row>

<row>
<entry>ORACLE_SID</entry>
<entry>ora8</entry>
<entry>This is an identifier for your Oracle server.</entry>
</row>

<row>
<entry>ORACLE_OWNER</entry>
<entry>oracle</entry>
<entry>The user who owns all of the oracle files.</entry>
</row>

<row>
<entry>ORACLE_GROUP</entry>
<entry>dba</entry>
<entry>The special oracle group. Users in the dba group are authorized to do a
<computeroutput>connect internal</computeroutput> within <computeroutput>svrmgrl</computeroutput> to gain full system
access to the Oracle system.</entry>
</row>
</tbody></tgroup></informaltable>


<para><phrase role="cvstag">($Id$)</phrase></para>
</sect2>
</sect1>

