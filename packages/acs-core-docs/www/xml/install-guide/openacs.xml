<sect1 id="openacs" xreflabel="Install OpenACS &version;">
<title>Install OpenACS &version;</title>

  <authorblurb>
	by <ulink url="mailto:vinod@kurup.com">Vinod Kurup</ulink>
  </authorblurb>

  <sect2>
    <title>Set up the file system for an OpenACS Service</title>
    <orderedlist>
      
      <listitem>
        <para>The reference install stores all OpenACS services in
      <computeroutput>/web</computeroutput>, with one subdirectory per
      service.  The first time you install a service, you must create
      that directory and set its permissions:</para>
        <screen>[root@yourserver root]# <userinput>mkdir /web</userinput>
[root@yourserver root]# <userinput>chgrp web /web</userinput>
[root@yourserver root]# <userinput>chmod 770 /web</userinput>
[root@yourserver root]#
<action>mkdir /web
chgrp web /web
chmod 770 /web</action></screen>

      </listitem>

      <listitem id="install-openacs-download">
        <para>You should already have downloaded the OpenACS tarball
      to the <computeroutput>/tmp</computeroutput> directory.  If
      noot, <link linkend="openacs-download">download the OpenACS
      tarball</link> and save it in
      <computeroutput>/tmp</computeroutput> and proceed:</para>

      </listitem>
      <listitem id="install-aolserver-user-accounts">
        <para>Set up your user account.</para>
        
        <para>
      AOLserver needs to be started as the root user if you want to use
      port 80. Once it starts, though, it will drop the root privileges and
      run as another user, which you must specify on the command line. It's
      important that this user has as few privileges as possible. Why?
      Because if an intruder somehow breaks in through AOLserver, you don't
      want her to have any ability to do damage to the rest of your
      server.</para>
      <para>At the same time, AOLserver needs to have write access to
      some files on your system in order for OpenACS to function
      properly. So, we'll run AOLserver with a different user account
      for each different service.  A service name should be a single
      word, <emphasis>letters and numbers only</emphasis>. If the name
      of your site is one word, that would be a good choice. For
      example &quot;<replaceable>service0</replaceable>&quot; might be the service name for the
      <ulink url="http://service0.net/"><replaceable>service0</replaceable>.net</ulink>
      community.</para>
      <para>For the &version;-P and &version;-O Reference Platform,
      we'll use a server named <replaceable>service0</replaceable> and
      a user named <replaceable>service0</replaceable>.  We'll leave the password
      blank for increased security.  The only way to log in will be
      with ssh certificates.  The only people who should log in are
      developers for that specific instance.  Add this user, and put
      it in the <computeroutput>web</computeroutput> group so that it
      can use database commands associated with that group.
    </para>

      <screen>[root@yourserver root]# <userinput>useradd -g web <replaceable>service0</replaceable></userinput>
[root@yourserver root]#</screen>
        
      <para>Set up database environment variables.  They are
	necessary for working with the database.  
</para>
      
      <screen>[root@yourserver root]# <userinput>su - <replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$ <userinput>emacs .bashrc</userinput></screen>
      <para>Put in the appropriate lines for the database you are running.  If you will use both databases, put in both sets of lines.</para>
      <itemizedlist>
        <listitem>
          <para>PostGreSQL:</para>
          <programlisting>export LD_LIBRARY_PATH=LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/usr/local/pgsql/lib
export PATH=$PATH:/usr/local/pgsql/bin</programlisting>
        </listitem>
        <listitem>
          <para>Oracle.  These environment variables are specific for a local Oracle
      installation communicating via IPC. If you are connecting to a remote
      Oracle installation, you'll need to adjust these appropriately. Also,
      make sure that the '8.1.7' matches your Oracle version.
</para>
          <programlisting>export ORACLE_BASE=/ora8/m01/app/oracle
export ORACLE_HOME=$ORACLE_BASE/product/<replaceable>8.1.7</replaceable>
export PATH=$PATH:$ORACLE_HOME/bin
export LD_LIBRARY_PATH=$ORACLE_HOME/lib:/lib:/usr/lib
export ORACLE_SID=ora8
export ORACLE_TERM=vt100
export ORA_NLS33=$ORACLE_HOME/ocommon/nls/admin/data</programlisting>
        </listitem>
      </itemizedlist>
      <para>Test this by logging out and back in as
	<computeroutput><replaceable>service0</replaceable></computeroutput> and checking the paths.</para>
      <screen>[service0@yourserver service0]$ <userinput>exit</userinput>
logout
[root@yourserver src]# <userinput>su - <userinput><replaceable>service0</replaceable></userinput></userinput>
[postgres@yourserver pgsql]$ <userinput>env | grep PATH</userinput>
</screen>
<para>For PostGreSQL, you should see:</para>
      <screen>
LD_LIBRARY_PATH=LD_LIBRARY_PATH=:/usr/local/pgsql/lib
PATH=/bin:/sbin:/usr/bin:/usr/sbin:/usr/local/bin:/usr/local/sbin:/usr/bin/X11:/usr/X11R6/bin:/root/bin:/usr/local/pgsql/bin:/usr/local/pgsql/bin</screen>
      <para>For Oracle:</para>
      <screen>ORACLE_BASE=/ora8/m01/app/oracle
ORACLE_HOME=/ora8/m01/app/oracle/product/8.1.7
PATH=/bin:/sbin:/usr/bin:/usr/sbin:/usr/local/bin:/usr/local/sbin:/usr/bin/X11:/usr/X11R6/bin:/root/bin:/ora8/m01/app/oracle/product/8.1.7/bin
LD_LIBRARY_PATH=/ora8/m01/app/oracle/product/8.1.7/lib:/lib:/usr/lib
ORACLE_SID=ora8
ORACLE_TERM=vt100
ORA_NLS33=$ORACLE_HOME/ocommon/nls/admin/data</screen>
      <screen>[service0@yourserver service0]$ <userinput>exit</userinput>
logout

[root@yourserver root]#</screen>
      </listitem>
      <listitem>
        <para>Unpack the OpenACS tarball and rename it to <computeroutput>service0</computeroutput>.  Secure the directory so that only the owner can access it.  Check the permissions by listing the directory.</para>       
        <screen>[root@yourserver root]# <userinput>su - <replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$ <userinput>cd /web</userinput>
[service0@yourserver web]$ <userinput>tar xzf /tmp/openacs-4-6.tgz</userinput>
[service0@yourserver web]$ <userinput>mv openacs-4-6 <replaceable>service0</replaceable></userinput>
[service0@yourserver web]$ <userinput>chmod -R 700 <replaceable>service0</replaceable></userinput>
[service0@yourserver web]$ ls -al
total 3
drwxrwx---    3 root     web          1024 Mar 29 16:41 .
drwxr-xr-x   25 root     root         1024 Mar 29 16:24 ..
drwx------    7 service0 web          1024 Jan  6 14:36 service0
[service0@yourserver web]$ <userinput>exit</userinput>
logout

[root@yourserver root]#
<action>su - service0
cd /web
tar xzf /tmp/openacs-4-6.tgz
mv openacs-4-6 service0
chmod -R 700 service0/
ls -al
exit</action></screen>
      </listitem>

      <listitem>
        <formalpara id="cvs-service-import">
        <title>Add the Service to CVS - OPTIONAL</title>
          <indexterm>
            <primary>cvs</primary>
            <secondary>setup</secondary>
          </indexterm>
        <para>If this is a development server, you may want to add it to your local CVS repository.</para>
        </formalpara>
        <orderedlist>
          <listitem>
        <para>Create and set permissions on a subdirectory in the local cvs repository.</para>
        <screen>[root@yourserver root]# <userinput>mkdir /cvsroot/<replaceable>service0</replaceable></userinput>
[root@yourserver root]#<userinput> chown <replaceable>service0</replaceable>.web /cvsroot/<replaceable>service0</replaceable></userinput>
[root@yourserver root]#
<action>mkdir /cvsroot/<replaceable>service0</replaceable>
chown <replaceable>service0</replaceable>.web /cvsroot/<replaceable>service0</replaceable></action></screen>
          </listitem>
          <listitem>
            <para>Add the repository location to the user environment.</para>
            <screen>[root@yourserver root]# <userinput>su - <replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$<userinput> emacs .bashrc</userinput></screen>
            <para>Put this string into <computeroutput>/home/<replaceable>service0</replaceable>/.bashrc</computeroutput>:</para>
            <programlisting>export CVSROOT=/cvsroot</programlisting>
            <screen>[service0@yourserver service0]$ <userinput>exit</userinput>
logout

[root@yourserver root]#</screen>
          </listitem>
          <listitem>
            <para>Import all files into cvs.  In order to work on
            files with source control, the files must be checked out
            from cvs.  So we will import, move aside, and then check
            out all of the files.  In the cvs import command,
            <computeroutput><replaceable>service0</replaceable></computeroutput>
            refers to the cvs repository to use; it uses the CVSROOT
            plus this string,
            i.e.
            <computeroutput>/cvsroot/<replaceable>service0</replaceable></computeroutput>.
            "OpenACS" is the vendor tag, and "openacs-4-6" is the
            release tag.  These tags will be useful in upgrading and
            branching.  -m sets the version comment.</para>
            <screen>[root@yourserver root]# <userinput>su - <replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$ <userinput>cd /web/<replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$ <userinput>cvs import -m "initial install" <replaceable>service0</replaceable> OpenACS openacs-4-6</userinput>
N <replaceable>service0</replaceable>/license.txt
N <replaceable>service0</replaceable>/readme.txt
<emphasis>(many lines omitted)</emphasis>
N <replaceable>service0</replaceable>/www/SYSTEM/flush-memoized-statement.tcl

No conflicts created by this import

[service0@yourserver service0]$
<action>su - <replaceable>service0</replaceable>
cd /web/<replaceable>service0</replaceable>
cvs import -m "initial install" <replaceable>service0</replaceable> OpenACS openacs-4-6</action></screen>
            <para>Move the original directory to a temporary location, and check out the cvs repository in its place.  If the service starts correctly, come back and remove the temporary copy of the uploaded files.</para>
            <screen>[service0@yourserver service0]$ <userinput>cd ..</userinput>
[service0@yourserver web]$<userinput> mv <replaceable>service0</replaceable> <replaceable>service0</replaceable>.orig</userinput>
[service0@yourserver web]$ <userinput>cvs checkout <replaceable>service0</replaceable></userinput>
cvs checkout: Updating <replaceable>service0</replaceable>
U <replaceable>service0</replaceable>/license.txt
<emphasis>(many lines omitted)</emphasis>
U <replaceable>service0</replaceable>/www/SYSTEM/dbtest.tcl
U <replaceable>service0</replaceable>/www/SYSTEM/flush-memoized-statement.tcl
[service0@yourserver web]$ <userinput>exit</userinput>
logout

[root@yourserver web]#
<action>cd ..
mv <replaceable>service0</replaceable> <replaceable>service0</replaceable>.orig
cvs checkout <replaceable>service0</replaceable>
exit</action></screen>
            
          </listitem>
        </orderedlist>
      </listitem>
      <listitem>
        <para>Set up several additional directories in the service root:
      <computeroutput>etc</computeroutput> is for configuration and control files, <computeroutput>log</computeroutput> is for error and request (web page hit) log files, and <computeroutput>database-backup</computeroutput> is for database backup files.  If you did the CVS step, note that these new directories are excluded from that step so that you can decide whether or not you want your logs and config files in source control.</para>
        <screen>[root@yourserver root]# <userinput>su - <replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$ <userinput>mkdir /web/<replaceable>service0</replaceable>/etc /web/<replaceable>service0</replaceable>/log /web/<replaceable>service0</replaceable>/database-backup</userinput>
[service0@yourserver web]$ <userinput>exit</userinput>
logout

[root@yourserver web]#
<action>su - service0
mkdir /web/<replaceable>service0</replaceable>/etc /web/<replaceable>service0</replaceable>/log /web/<replaceable>service0</replaceable>/database-backup
exit</action></screen>
      </listitem>
    </orderedlist>
  </sect2>
  
  <sect2 id="install-openacs-prepare-oracle" xreflabel="Prepare Oracle for OpenACS">
    <title>Prepare Oracle for OpenACS</title>
    
    <para>OPTIONAL - if you won't be using Oracle, skip to <xref
	  linkend="install-openacs-prepare-postgres"/></para>
    <para>
	  You should be sure that your user account
	  (e.g. <computeroutput><replaceable>service0</replaceable></computeroutput>) is in the
	  <computeroutput>dba</computeroutput> group.
	</para>
    <orderedlist>
      <listitem><para> 
		  Verify membership by typing
		  <computeroutput>groups</computeroutput> when you login:

		  <programlisting>
<replaceable>service0</replaceable>:~$ groups
dba web</programlisting>

		  If you do not see these groups, take the following action: 

		  <programlisting>
<replaceable>service0</replaceable>:~$ su -      
Password: ************
root:~# adduser <emphasis><replaceable>service0</replaceable></emphasis> dba</programlisting>

		  If you get an error about an undefined group, then add that group
		  manually:

<programlisting>
root:~# groupadd dba
root:~# groupadd web</programlisting>

		  Make sure to logout as <computeroutput>root</computeroutput> when
		  you are finished with this step and log back in as
		  your regular user.
		</para></listitem>

      <listitem><para> 
		  Connect to Oracle using
		  <computeroutput>svrmgrl</computeroutput> and login:
        
		  <programlisting>
<replaceable>service0</replaceable>:~$ svrmgrl

SVRMGR&gt; connect internal
Connected.</programlisting>
		</para></listitem>

      <listitem><para>
		  Determine where the system tablespaces are stored: 

		  <programlisting>
SVRMGR&gt;  select file_name from dba_data_files;</programlisting>
        Example results: 

		  <programlisting>
/ora8/m01/app/oracle/oradata/ora8/system01.dbf
/ora8/m01/app/oracle/oradata/ora8/tools01.dbf
/ora8/m01/app/oracle/oradata/ora8/rbs01.dbf
/ora8/m01/app/oracle/oradata/ora8/temp01.dbf
/ora8/m01/app/oracle/oradata/ora8/users01.dbf
/ora8/m01/app/oracle/oradata/ora8/indx01.dbf
/ora8/m01/app/oracle/oradata/ora8/drsys01.dbf</programlisting>
		</para></listitem>

      <listitem><para> 
		  Using the above output, you should determine where
		  to store your tablespace. As a general rule, you&#39;ll want to
		  store your tablespace on a mount point under the
		  <computeroutput>/ora8</computeroutput> directory that is separate
		  from the Oracle system data files. By default, the Oracle system
		  is on <computeroutput>m01</computeroutput>, so we will use
		  <computeroutput>m02</computeroutput>. This enables your Oracle
		  system and database files to be on separate disks for optimized
		  performance. For more information on such a configuration, see
		  <ulink
		  url="http://philip.greenspun.com/panda/databases-choosing">Chapter
		  12</ulink> of <ulink
		  url="http://philip.greenspun.com/panda/">Philip&#39;s
		  book</ulink>.  For this example, we&#39;ll use
		  <computeroutput>/ora8/m02/oradata/ora8/</computeroutput>.
		</para></listitem>

      <listitem><para> 
		  Create the directory for the datafile; to do this,
		  exit from <computeroutput>svrmgrl</computeroutput> and login as
		  <computeroutput>root</computeroutput> for this step: </para>

		<programlisting>
SVRMGR&gt; exit
<replaceable>service0</replaceable>:~$ su -
Password: ************
root:~# mkdir -p /ora8/m02/oradata/ora8/
root:~# chown <emphasis><replaceable>service0</replaceable></emphasis>.web /ora8/m02/oradata/ora8
root:~# chmod 775 /ora8/m02/oradata/ora8
root:~# exit
<replaceable>service0</replaceable>:~$</programlisting>
      </listitem>

      <listitem><para> 

		  Create a tablespace for the service. It is important that the
		  tablespace can <computeroutput>autoextend</computeroutput>. This
		  allows the tablespace&#39;s storage capacity to grow as the size
		  of the data grows. We set the pctincrease to be a very low value
		  so that our extents won&#39;t grow geometrically. We do not set
		  it to 0 at the tablespace level because this would affect
		  Oracle&#39;s ability to automatically coalesce free space in the
		  tablespace.

		</para>
		<programlisting>
<replaceable>service0</replaceable>:~$ svrmgrl

SVRMGR&gt; connect internal;
SVRMGR&gt; create tablespace <emphasis><replaceable>service0</replaceable></emphasis> 
             datafile &#39;/ora8/m02/oradata/ora8/<emphasis><replaceable>service0</replaceable></emphasis>01.dbf&#39; 
             size 50M 
             autoextend on 
             next 10M
             maxsize 300M
             extent management local
             uniform size 32K;</programlisting>
      </listitem>

      <listitem><para> 
		  Create a database user for this service. Give the
		  user access to the tablespace and rights to connect. We'll use
		  <computeroutput><replaceable>service0</replaceable>password</computeroutput> as our password.</para>

        <para>
		  Write down what you specify as <emphasis>service_name</emphasis>
			(i.e. <computeroutput><replaceable>service0</replaceable></computeroutput>) and
			<emphasis>database_password</emphasis>
			(i.e. <computeroutput><replaceable>service0</replaceable>password</computeroutput>). You
			will need this information for configuring exports and
			AOLserver.
		</para>

		<programlisting>
SVRMGR&gt; create user <emphasis><replaceable>service0</replaceable></emphasis> identified by <emphasis><replaceable>service0</replaceable>password</emphasis> default tablespace <emphasis><replaceable>service0</replaceable></emphasis>
temporary tablespace temp quota unlimited on <emphasis><replaceable>service0</replaceable></emphasis>;
SVRMGR&gt; grant connect, resource, ctxapp, javasyspriv, query rewrite to <emphasis><replaceable>service0</replaceable></emphasis>;
SVRMGR&gt; revoke unlimited tablespace from <emphasis><replaceable>service0</replaceable></emphasis>;
SVRMGR&gt; alter user <emphasis><replaceable>service0</replaceable></emphasis> quota unlimited on <emphasis><replaceable>service0</replaceable></emphasis>;
SVRMGR&gt; exit;</programlisting>

        <para>
		  Your table space is now ready. In case you are trying to delete a
		  previous OpenACS installation, consult these commands in <xref
		  linkend="install-openacs-delete-tablespace"/> below.
		</para></listitem>

      <listitem><para>
        Make sure that you can login to Oracle using your
        <emphasis>service_name</emphasis> account: </para>

		<programlisting>
<replaceable>service0</replaceable>:~$ sqlplus <emphasis><replaceable>service0</replaceable></emphasis>/<emphasis><replaceable>service0</replaceable>password</emphasis>
SQL&gt; select sysdate from dual;

SYSDATE
----------
2001-12-20

SQL&gt; exit</programlisting>

        <para>
		  You should see today&#39;s date in a format &#39;YYYY-MM-DD.&#39;
		  If you can&#39;t login, try redoing step 1 again. If the date is
		  in the wrong format, make sure you followed the steps outlined in
		  <xref linkend="install-oracle-troubleshooting"/>
		  </para></listitem>
      
    </orderedlist>
  </sect2>

  <sect2 id="install-openacs-prepare-postgres" xreflabel="Prepare PostgreSQL for OpenACS">
    <title>Prepare PostgreSQL for an OpenACS Service</title>
    <orderedlist>
      <listitem>
        <para>Create a user in the database matching the service name.</para>
        <screen>[root@yourserver root]# <userinput>su - postgres</userinput>
[postgres@yourserver pgsql]$ <userinput>createuser <replaceable>service0</replaceable></userinput>
Shall the new user be allowed to create databases? (y/n) <userinput>y</userinput>
Shall the new user be allowed to create more new users? (y/n) <userinput>y</userinput>
CREATE USER
[postgres@yourserver pgsql]$ <userinput>exit</userinput>
logout

[root@yourserver root]#</screen>
      </listitem>
      <listitem>
	<para>Create a database with the same name as our service name, <replaceable>service0</replaceable>.</para>
    <screen>[root@yourserver root]# <userinput>su - <replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$ <userinput>createdb <replaceable>service0</replaceable></userinput>
CREATE DATABASE
[service0@yourserver service0]$
<action>su - <replaceable>service0</replaceable>
createdb <replaceable>service0</replaceable></action></screen>
      </listitem>
      <listitem>
        <para>Automate daily database Vacuuming.  This is a process which cleans out discarded data from the database.  A quick way to automate vacuuming is to edit the cron file for the database user.</para>
        <indexterm>
          <primary>Postgres</primary>
          <secondary>Vacuuming</secondary>
        </indexterm>
        <screen>[service0@yourserver service0]$ <userinput>export EDITOR=emacs;crontab -e</userinput></screen>
        <para>Add this line to the file.  The numbers and stars at the beginning are cron columns that specify when the program should be run - in this case, whenever the minute is 0 and the hour is 1, i.e., 1:00 am every day.</para>
        <programlisting>0 1 * * * /usr/local/pgsql/bin/vacuumdb <replaceable>service0</replaceable></programlisting>
      </listitem>
      <listitem id="install-next-openfts">
        <para>Add Full Text Search Support - OPTIONAL</para>
        <indexterm>
          <primary>full text search</primary>
          <secondary>installation</secondary>
        </indexterm>
        <para>If you are installing Full Text Search, add required packages to the new database.</para>
        <screen>[service0@yourserver service0]$ <userinput>/usr/local/pgsql/bin/psql <replaceable>service0</replaceable> -f /usr/local/src/postgresql-7.2.3/contrib/tsearch/tsearch.sql</userinput>
BEGIN
CREATE
<emphasis>(many lines omitted)</emphasis>
INSERT 0 1
COMMIT
[service0@yourserver service0]$ <userinput>/usr/local/pgsql/bin/psql <replaceable>service0</replaceable> -f /usr/local/src/postgresql-7.2.3/contrib/pgsql_contrib_openfts/openfts.sql</userinput>
CREATE
CREATE
[service0@yourserver service0]$
<action>/usr/local/pgsql/bin/psql <replaceable>service0</replaceable> -f /usr/local/src/postgresql-7.2.3/contrib/tsearch/tsearch.sql
/usr/local/pgsql/bin/psql <replaceable>service0</replaceable> -f /usr/local/src/postgresql-7.2.3/contrib/pgsql_contrib_openfts/openfts.sql</action></screen>
      </listitem>
      <listitem>
        <screen>[service0@yourserver service0]$ <userinput>exit</userinput>
logout

[root@yourserver root]# </screen>
      </listitem>
    </orderedlist>
  </sect2>
  
  <sect2 id="install-openacs-configure-aol">
    <title>Configure an AOLserver Service for OpenACS</title>
    <orderedlist>
      <listitem>
        <para>
	  The AOLserver architecture lets you run an arbitrary number of
	  virtual servers. A virtual server is an HTTP service running on a
	  specific port, e.g. port 80. In order for OpenACS to work, you
	  need to configure a virtual server.  The Reference Platform uses a configuration file included in the OpenACS tarball.  Copy it to the <computeroutput>/web/<replaceable>service0</replaceable>/etc</computeroutput> directory and open it in an editor to adjust the parameters.</para>
        <indexterm>
          <primary>AOLserver</primary>
          <secondary>configuration</secondary>
        </indexterm>
    
        <screen>[root@yourserver root]# <userinput>su - <replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$ <userinput>cd /web/<replaceable>service0</replaceable>/etc</userinput>
[service0@yourserver etc]# <userinput>cp /web/<replaceable>service0</replaceable>/packages/acs-core-docs/www/files/config.tcl.txt config.tcl</userinput>
[service0@yourserver etc]# <userinput>emacs config.tcl</userinput>
</screen>
    
    <para>
	   You can continue without changing any values in the file.  However, if you don't change <computeroutput>address</computeroutput> to match the computer's ip address, you won't be able to browse to your server from other machines.
	</para>
    
    <itemizedlist>
      <listitem><para><emphasis>httpport</emphasis> - If you want your
		  server on a different port, enter it here.  The Reference Platform port is 8000, which is suitable for development use.  Port 80 is the standard http port - it's the port used by your browser when you enter http://yourserver.test.  So you should use port 80 for your production site.</para></listitem>
      
      <listitem>
        <para><emphasis>httpsport</emphasis> - This is the
      port for https requests.  The Reference Platform https port is
      8443.  If http port is set to 80, httpsport should be 143 to
      match the standard.</para>
      </listitem>
      
      <listitem><para> 
		  <emphasis>address</emphasis> - The IP address of the server.  If you are hosting multiple IPs on one computer, this is the address specific to the web site.  Each virtual server will ignore any requests directed at other addresses.</para>
      </listitem>
      <listitem>
        <para><emphasis>server</emphasis> - This is the keyword that, by convention, identifies the service.  It is also used as part of the path for the service root, as the name of the user for running the service, as the name of the database, and in various dependent places.  The Reference Platform uses <replaceable>service0</replaceable>.
      
    </para></listitem>
      
      <listitem><para><emphasis>db_name</emphasis> - In almost all cases,
		  this can be kept as a reference to $server. If for some reason,
		  the tablespace you are using is different than your servername,
		  then you can set it here. You should have a good reason for doing
		  this. 
		</para></listitem>

      <listitem><para> 
		  <emphasis>servername</emphasis> - This is just a *pretty* name for your server.</para>
          </listitem>
          
          <listitem>
            <para><emphasis>user_account</emphasis> - The account that
        will both own OpenACS files and connect to the database (for
        Postgresql).</para>
          </listitem>
          
          <listitem>
            <para><emphasis>debug</emphasis> - Set to true for a very verbose error log, including many lines for every page view, success or failure.</para>
          </listitem>
          
        </itemizedlist>
    
    <para>
	  AOLServer is very configurable. These settings should get you
	  started, but for more options, read the <ulink
	  url="http://aolserver.com/docs/admin/config.adp">AOLServer
	  docs</ulink>.
	</para>
      </listitem>
      <listitem>
        <para>OPTIONAL: To run OpenFTS, uncomment this line from <computeroutput>config.tcl</computeroutput>.  (To uncomment a line in a tcl file, remove the <computeroutput>#</computeroutput> at the beginning of the line.)</para>
        <programlisting>#ns_param   nsfts           ${bindir}/nsfts.so</programlisting>
      </listitem>
      <listitem>
        <para>OPTIONAL: To run nsopenssl:</para>
        <orderedlist>
          <listitem>
            <para>Uncomment this line from <computeroutput>config.tcl</computeroutput>.</para>
            <programlisting>#ns_param   nsopenssl       ${bindir}/nsopenssl.so
</programlisting>
          </listitem>
          <listitem id="ssl-certificates" linkref="Generate ssl certificates">
            <para>Prepare a certificate directory for the service.</para>
            <screen>[service0@yourserver etc]$ <userinput>mkdir /web/<replaceable>service0</replaceable>/etc/certs</userinput>
[service0@yourserver etc]$ <userinput>chmod 700 /web/<replaceable>service0</replaceable>/etc/certs</userinput>
[service0@yourserver etc]$ 
<action>mkdir /web/<replaceable>service0</replaceable>/etc/certs
chmod 700 /web/<replaceable>service0</replaceable>/etc/certs</action></screen>            
          </listitem>
          <listitem>
            <para>It takes two files to support an SSL connection.  The certificate is the public half of the key pair - the server sends the certificate to browser requesting ssl.  The key is the private half of the key pair.  In addition, the certificate must be signed by Certificate Authority or browsers will protest.  Each web browser ships with a built-in list of acceptable Certificate Authorities (CAs) and their keys.  Only a site certificate signed by a known and approved CA will work smoothly.  Any other certificate will cause browsers to produce some messages or block the site.  Unfortunately, getting a site certificate signed by a CA costs money.  In this section, we'll generate an unsigned certificate which will work in most browsers, albeit with pop-up messages.</para>
            <para>Use an OpenSSL perl script to generate a certificate and key.</para>
            <screen>[service0@yourserver service0]$ <userinput>cd /web/service0/etc/certs</userinput>
[service0@yourserver certs]$ <userinput>perl /usr/share/ssl/misc/CA -newcert</userinput>
Using configuration from /usr/share/ssl/openssl.cnf
Generating a 1024 bit RSA private key
...++++++
.......++++++
writing new private key to 'newreq.pem'
Enter PEM pass phrase:</screen>
            <para>Enter a pass phrase for the CA certificate.  Then, answer the rest of the questions.  At the end you should see this:</para>
        <screen>Certificate (and private key) is in newreq.pem
[service0@yourserver certs]$</screen>
        <para><computeroutput>newreq.pem</computeroutput> contains our certificate and private key.  The key is protected by a passphrase, which means that we'll have to enter the pass phrase each time the server starts.  This is impractical and unnecessary, so we create an unprotected version of the key.  <emphasis>Security implication</emphasis>: if anyone gets access to the file keyfile.pem, they effectively own the key as much as you do.  Mitigation: don't use this key/cert combo for anything besides providing ssl for the web site.</para>
            <screen>[root@yourserver misc]# <userinput>openssl rsa -in newreq.pem -out keyfile.pem</userinput>
read RSA key
Enter PEM pass phrase:
writing RSA key
[service0@yourserver certs]$ </screen>
        <para>To create the certificate file, we take the combined file, copy it, and strip out the key.</para>
        <screen>[service0@yourserver certs]$ <userinput>cp newreq.pem certfile.pem</userinput>
[root@yourserver misc]# <userinput>emacs certfile.pem</userinput></screen>
        <para>Strip out the section that looks like</para>
        <programlisting>-----BEGIN RSA PRIVATE KEY-----
Proc-Type: 4,ENCRYPTED
DEK-Info: DES-EDE3-CBC,F3EDE7CA1B404997
S/Sd2MYA0JVmQuIt5bYowXR1KYKDka1d3DUgtoVTiFepIRUrMkZlCli08mWVjE6T
<emphasis>(11 lines omitted)</emphasis>
1MU24SHLgdTfDJprEdxZOnxajnbxL420xNVc5RRXlJA8Xxhx/HBKTw==
-----END RSA PRIVATE KEY-----</programlisting>
          </listitem>
        </orderedlist>
      </listitem>
    </orderedlist>
  </sect2>

  <sect2>
    <title>Verify AOLserver startup</title>
    <orderedlist>
      <listitem>
        <para>
	  Kill any current running AOLserver processes and start a new
	  one. (Note, if you are using Oracle, rather than PostgreSQL, replace
	  <computeroutput>nsd-postgres</computeroutput> with
	  <computeroutput>nsd-oracle</computeroutput>).  If you are using port 80, you must be root for this step. </para>
        <screen>[service0@yourserver etc]$ <userinput>killall nsd</userinput>
nsd: no process killed
[service0@yourserver service0]$<userinput> /usr/local/aolserver/bin/nsd-postgres -t /web/<replaceable>service0</replaceable>/etc/config.tcl</userinput>
[service0@yourserver service0]$ [08/Mar/2003:18:13:29][32131.8192][-main-] Notice: nsd.tcl: starting to read config file...
[08/Mar/2003:18:13:29][32131.8192][-main-] Notice: nsd.tcl: finished reading config file.</screen>
      </listitem>
      <listitem>
        <para>
	  Attempt to connect to the service from a web browser as you did
	  You should specify a URL like:
	</para>
	<computeroutput>http://<replaceable>yourserver.test</replaceable>:8000</computeroutput>
	<para>
	  You should see a page that looks like <ulink
        url="files/openacs-start.html">this</ulink>.  If you <link
        linkend="cvs-service-import">imported your files into
        cvs</link>, now that you know it worked you can erase the temp
        directory with <computeroutput>rm -rf /web/service0.orig</computeroutput>.
	</para>

	<para>

	  If you don&#39;t see the login page, view your error log
	  (<computeroutput>/web/<replaceable>service0</replaceable>/log/<replaceable>service0</replaceable>-error.log</computeroutput>)
	  to make sure the service is starting without any problems. If you
	  need to make changes, don&#39;t forget to kill any running servers with <userinput>killall nsd</userinput>.

	</para>
      </listitem>
      <listitem id="install-openacs-keepalive" xreflabel="Keep AOLServer alive">
        <para>OPTIONAL - Automate AOLserver keepalive</para>
        
        <para>Assuming AOLserver started cleanly in the previous step, we'll set it up so that it's always running, and automatically restarts whenever it dies or is stopped.  This step is strongly recommended, even for development sites, because it makes install and maintenance much simpler.  </para>

        <para>The Reference Platform uses Daemontools to control AOLserver.  An earlier method using <computeroutput>init</computeroutput>, less flexible and reliable, is <link linkend="install-openacs-inittab">here</link>.</para>
        <orderedlist>
          <listitem>
            <para>Daemontools must already be installed.  If not, <link linkend="install-daemontools">install it</link>.</para>
          </listitem>
          <listitem>
            <para>Each service controlled by daemontools must have a directory in <computeroutput>/service</computeroutput>.  That directory must have a file called <computeroutput>run</computeroutput>.  Daemontools then creates additional files and directories to track status and log.  Create the appropriate directory as <computeroutput>/web/<replaceable>service0</replaceable>/etc/daemontools</computeroutput>, copy the prepared <computeroutput>run</computeroutput> file, and set permissions.  If your server is not called <replaceable>service0</replaceable>, edit <computeroutput>/web/<replaceable>service0</replaceable>/etc/run accordingly.</computeroutput></para>
        <screen>[service0@yourserver log]$ <userinput>cd /web/<replaceable>service0</replaceable>/etc</userinput>
[service0@yourserver etc]$ <userinput>mkdir daemontools</userinput>
[service0@yourserver etc]$ <userinput>cp /web/<replaceable>service0</replaceable>/packages/acs-core-docs/www/files/run.txt daemontools/run</userinput>
[service0@yourserver etc]$ <userinput>chmod 700 daemontools/run</userinput>
<action>cd /web/<replaceable>service0</replaceable>/etc
mkdir daemontools
cp /web/<replaceable>service0</replaceable>/packages/acs-core-docs/www/files/run.txt daemontools/run
chmod 700 daemontools/run</action></screen>
          </listitem>
          <listitem>
            <para>Kill any existing AOLserver instances.  As root, link the <computeroutput>daemontools</computeroutput> directory into the <computeroutput>/service</computeroutput> directory.  Daemontools' <computeroutput>svscan</computeroutput> process checks this directory every five seconds, and will quickly execute <computeroutput>run</computeroutput>.</para>
            
            <screen>[service0@yourserver etc]$ <userinput>killall nsd</userinput>
nsd: no process killed
[service0@yourserver etc]$ <userinput>exit</userinput>

[root@yourserver root]# <userinput>ln -s /web/<replaceable>service0</replaceable>/etc/daemontools/ /service/<replaceable>service0</replaceable></userinput></screen>
            <para>Verify that AOLserver is running.</para>
            <screen>[root@yourserver root]#<userinput> ps -auxw | grep nsd</userinput>
<replaceable>service0</replaceable>   5562 14.2  6.2 22436 15952 ?       S    11:55   0:04 /usr/local/aolserver/bin/nsd -it /web/<replaceable>service0</replaceable>/etc/config.tcl -u serve
root      5582  0.0  0.2  3276  628 pts/0    S    11:55   0:00 grep nsd
[root@yourserver root]#</screen>
          </listitem>
          <listitem>
            <para>The user <replaceable>service0</replaceable> can now control the service <replaceable>service0</replaceable> with these commands:</para>
            <itemizedlist>
              <listitem><para>

            <computeroutput>svc -d /service/<replaceable>service0</replaceable></computeroutput> -
            Bring the server down

          </para></listitem>
              
              <listitem><para>

            <computeroutput>svc -u /service/<replaceable>service0</replaceable></computeroutput> -
            Start the server up and leave it in keepalive mode.

          </para></listitem>
              
              <listitem><para>

            <computeroutput>svc -o /service/<replaceable>service0</replaceable></computeroutput> -
            Start the server up once. Do not restart it if it stops.

          </para></listitem>

        <listitem><para>

            <computeroutput>svc -t /service/<replaceable>service0</replaceable></computeroutput> -
            Stop and immediately restart the server.

          </para></listitem>

        <listitem><para>
        
            <computeroutput>svc -k /service/<replaceable>service0</replaceable></computeroutput> -
            Sends the server a KILL signal. This is like KILL -9. AOLserver
            exits immediately. If svc -t fails to fully kill AOLserver, use
            this option.  This does not take the server out of keepalive mode, so it should still bounce back up immediately.

          </para></listitem>
        </itemizedlist>
      </listitem>
      <listitem>
        <para>
        At this point, these commands will work only for the
        <computeroutput>root</computeroutput> user.  Grant permission for the <computeroutput>web</computeroutput> group to use <computeroutput>svc</computeroutput> commands on the <emphasis><replaceable>service0</replaceable></emphasis> server.</para>
            <screen>[root@yourserver root]# <userinput>svgroup web /service/<replaceable>service0</replaceable></userinput>
[root@yourserver root]#</screen>
          </listitem>
          <listitem>
            <para>Verify that the controls work.  You may want to <computeroutput>tail -f /web/<replaceable>service0</replaceable>/log/<replaceable>service0</replaceable>-error.log</computeroutput> in another window, so you can see what happens when you type these commands.
      </para>
            <para>

        Most of this information comes from Tom Jackson's <ulink
          url="http://zmbh.com/daemontools-aolserver/aolserver+daemontools.html">AOLServer+Daemontools
          Mini-HOWTO</ulink>.
</para>
          </listitem>
        </orderedlist>
      </listitem>
    </orderedlist>
  </sect2>
  
  <sect2 id="install-openacs-using-installer" xreflabel="Using the OpenACS Installer">
    <title>Configure a Service with the OpenACS Installer</title>
    
    <para>
	  Now that you've got AOLserver up and running, let's install OpenACS
	  &version;.
	</para>

    <itemizedlist>
      <listitem><para> 
		  You should see a page from the webserver titled
		  <computeroutput>OpenACS Installation:
		  Welcome</computeroutput>. You will be warned if your version of
		  the database driver is out of date, if AOLserver cannot connect
		  to the database, if any modules are missing or out-of-date, or if
		  there are any problems with filesystem permissions on the server
		  side. But if everything is fine, you can click
		  <computeroutput>Next</computeroutput> to proceed to load the
		  OpenACS Kernel data model.  
		</para></listitem>

      <listitem><para>

		  The next page shows the results of loading the OpenACS Kernel
		  data model - be prepared to wait a few minutes as it works. You
		  should see a string of output messages from the database as the
		  datamodel is created.  You'll see the line:

		</para>

		<programlisting>
Loading package .info files ... this will take a few minutes</programlisting>

		<para>

		  This will really take a few minutes. Have faith! Finally, another
		  <computeroutput>Next</computeroutput> button will appear at the
		  bottom - click it.

		</para>
	  </listitem>

      <listitem><para> 		  

		  The following page shows the results of loading the core package
		  data models. You should see positive results for each of the
		  previously selected packages, but watch out for any
		  errors. Eventually, the page will display &quot;Generating secret
		  tokens&quot; and then &quot;Done&quot;- click
		  <computeroutput>Next</computeroutput>.

		</para></listitem>

      <listitem><para>

		  You should see a page, &quot;OpenACS Installation: Create
		  Administrator&quot; with form fields to define the OpenACS site
		  administrator. Fill out the fields as appropriate, and click
		  <computeroutput>Create User</computeroutput>.

		</para></listitem>

      <listitem><para>
		  You should see a page, &quot;OpenACS Installation: Set System
		  Information&quot; allowing you to name your service. Fill out the
		  fields as appropriate, and click <computeroutput>Set System
		  Information</computeroutput> 
		</para></listitem>

      <listitem><para> 
		  You&#39;ll see the final Installer page, &quot;OpenACS
		  Installation: Complete.&quot; It will tell you that the server is
		  being restarted; note that unless you already set up a way for
		  AOLServer to restart itself (ie. inittab or daemontools),
		  you&#39;ll need to manually restart your service.
		</para>
        <screen>[service0@yourserver service0]$ <userinput>/usr/local/aolserver/bin/nsd-postgres -t /web/<emphasis><replaceable>service0</replaceable></emphasis>/config.tcl</userinput></screen>
	  </listitem>

      <listitem><para> 
		  Give the server a few minutes to start up. Then
		  reload the final page above. You should see the front page, with
		  an area to login near the upper right.  Congratulations, OpenACS
		  &version; is now up and running!  
		</para></listitem>


      <listitem><para>OPTIONAL - Install Full Text Search.</para>
        <orderedlist>

          <listitem>
            <para>Click <computeroutput><guilabel>Package Manager</guilabel></computeroutput> on the right side of the default home page.  If prompted, log in with the account and password you entered during install.</para>
          </listitem>

          <listitem><para>Click on the <computeroutput><guilabel>Install
packages</guilabel></computeroutput> link.</para>
          </listitem>

	<listitem><para>On the next screen, after it loads, click on <computeroutput><guilabel>Uncheck all boxes</guilabel></computeroutput>, then click the second checkbox next to <computeroutput><guilabel>OpenFTS Driver 4.2</guilabel></computeroutput>.  This will automatically check the first box. Then click <computeroutput><guibutton><accel>N</accel>ext</guibutton></computeroutput>. </para></listitem>
          
          <listitem><para>Click <computeroutput><guibutton>Install Packages</guibutton></computeroutput></para></listitem>
          
          <listitem><para>Restart the service.</para>
<screen>[service0@yourserver service0]$ <userinput>svc -t /service/<replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$</screen></listitem>
          
          <listitem><para>Wait a minute, then browse back to the home page.</para>
          </listitem>

          <listitem><para>Click on <computeroutput><guilabel>Site Map</guilabel></computeroutput> on the top right side of the screen.</para>
          </listitem>
          
          <listitem>
            <para>Mount the OpenFTS Full Text Search Engine in the site map.</para>
            <orderedlist>
              <listitem><para>Click the <computeroutput><guilabel>new sub folder</guilabel></computeroutput> link on the "/" line, the first line under Main Site:/.</para></listitem>
              <listitem><para>Type <userinput>openfts</userinput>
and click <computeroutput><guibutton>New</guibutton></computeroutput>.</para></listitem>
              <listitem><para>On the new <computeroutput><guilabel>openfts</guilabel></computeroutput> line, click the <computeroutput><guilabel>mount</guilabel></computeroutput> link.</para></listitem>
              <listitem><para>Click <computeroutput><guilabel>OpenFTS
Driver</guilabel></computeroutput>.</para></listitem>
              <listitem><para>On the <computeroutput><guilabel>openfts</guilabel></computeroutput> line, click <computeroutput><guilabel>set parameters</guilabel></computeroutput>.</para>
              </listitem>
              <listitem><para>Change <computeroutput><guilabel>openfts_tcl_src_path</guilabel></computeroutput> to <userinput>/usr/local/src/Search-OpenFTS-tcl-0.3.2/</userinput> and click <computeroutput><guibutton>Set Parameters</guibutton></computeroutput>
              </para>
              </listitem>
            </orderedlist>
          </listitem>
          <listitem>
            <para>Mount the Search interface in the site map.</para>
            <orderedlist>
              <listitem><para>Click the
<computeroutput><guilabel>new sub folder</guilabel></computeroutput> link on the
Main Site line. </para></listitem>
              <listitem><para>Type <userinput>search</userinput>
and click <computeroutput><guibutton>New</guibutton></computeroutput>. </para></listitem>
              <listitem><para>Click the <computeroutput><guilabel>new
application</guilabel></computeroutput> link on the <computeroutput><guilabel>search</guilabel></computeroutput>
	  line. </para></listitem>
              <listitem><para>Type <userinput>search</userinput>
where it says
<computeroutput><guilabel>untitled</guilabel></computeroutput>, choose
<computeroutput><guilabel>search</guilabel></computeroutput> from the
drop-down list, and click
<computeroutput><guibutton>New</guibutton></computeroutput>.
</para></listitem>
            </orderedlist>
          </listitem>
          <listitem>
            <para>Restart the service.</para>
            <screen>[service0@yourserver service0]$ <userinput>svc -t /service/<replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$</screen>
          </listitem>
          <listitem><para>Wait a minute, then click on <computeroutput><guilabel>Main Site</guilabel></computeroutput> at the top of the page.</para>
          </listitem>
          <listitem>
            <para>Initialize the OpenFTS Engine.  This creates a set of tables in the database to support FTS.</para>
            <para>Near the bottom of the page, click on the <computeroutput><guilabel>OpenFTS Driver</guilabel></computeroutput> link. Click on <computeroutput><guilabel>Administration</guilabel></computeroutput>.
Click on <computeroutput><guilabel>Initialize OpenFTS Engine</guilabel></computeroutput>. 
Click <computeroutput><guibutton>Initialize OpenFTS Engine</guibutton></computeroutput>. </para>
          </listitem>
          <listitem>
            <para>Add the FTS Engine service contract</para>
            <orderedlist>
              <listitem><para>Click on the <computeroutput><guilabel>Main
Site</guilabel></computeroutput>. </para></listitem>
              <listitem><para>Click on the <computeroutput><guilabel>ACS
Service Contract</guilabel></computeroutput> link near the bottom of the home page. </para></listitem>
              <listitem><para>On the <computeroutput><guilabel>FtsEngineDriver</guilabel></computeroutput>
line, click
<computeroutput><guilabel>Install</guilabel></computeroutput>.
</para></listitem>
            </orderedlist>
          </listitem>
          <listitem>
            <para>Restart the service.</para>
            <screen>[service0@yourserver service0]$ <userinput>svc -t /service/<replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$</screen>
          </listitem>
          <listitem>
            <para>Test FTS.  (INCOMPLETE).  Add a package that supports search,like "note," add some content, and search for it.</para>
          </listitem>
        </orderedlist>
      </listitem>
    </itemizedlist>
  </sect2>

  <sect2>
    <title>Back up the New Service - OPTIONAL</title>
    <para>This is a very good time to back the service, even if it's not a production service.  Making a backup now lets you roll back to this initial, clean setup at any point in the future, without repeating the install process.  A full OpenACS service backup includes everything in the <computeroutput>/web/<replaceable>service0</replaceable>/</computeroutput> directory.  At this point it's probably sufficient to back up just the database, because you can recover the files from a tarball.</para>
    <para>Note that, if you did the CVS options in this document, the <computeroutput>/web/service0/etc</computeroutput> directory is not included in cvs and you may want to add it.</para>
    <itemizedlist>
      <listitem>
        <formalpara>
          <title>PostGreSQL</title>
          <para>Create a backup file and verify that it was created and has a reasonable size (several megabytes).</para>
        </formalpara>
        <screen>[service0@yourserver service0]$ <userinput>mkdir /web/<replaceable>service0</replaceable>/database-backup</userinput>
[service0@yourserver service0]$ <userinput>pg_dump -f /web/<replaceable>service0</replaceable>/database-backup/initial_backup.dmp <replaceable>service0</replaceable></userinput>
[service0@yourserver service0]$ <userinput>ls -al /web/<replaceable>service0</replaceable>/database-backup</userinput>
total 1425
drwxr-xr-x    2 <replaceable>service0</replaceable>  web          1024 Mar  9 14:13 .
drwx------   11 <replaceable>service0</replaceable>  web          1024 Mar  9 14:11 ..
-rw-r--r--    1 <replaceable>service0</replaceable>  web       1449826 Mar  9 14:13 initial_backup.dmp
[service0@yourserver service0]$
<action>mkdir /web/<replaceable>service0</replaceable>/database-backup
pg_dump -f /web/<replaceable>service0</replaceable>/database-backup/initial_backup.dmp <replaceable>service0</replaceable>
ls -al /web/<replaceable>service0</replaceable>/database-backup</action></screen>
      </listitem>
      <listitem>
        <formalpara>
          <title>Oracle - INCOMPLETE</title>
          <para></para>
        </formalpara>
      </listitem>
    </itemizedlist>
  </sect2>

  <sect2>
    <title>Set up Automated Backup - OPTIONAL</title>
    <para>Backup can encompass all files in <computeroutput>/web/service0</computeroutput>.  For a development server, putting the files in cvs is sufficient.  (It's important then to back up the cvs repository!)</para>
    
    <para>A quick way to automate database backup is a cron job.  This is not recommended for production and is not part of the Reference Platform, because it is not cross-platform and can fail silently.  More thorough methods are documented in <xref linkend="install-next-backups"/></para>
    <screen>[service0@yourserver service0]$ <userinput>export EDITOR=emacs;crontab -e</userinput></screen>
    <para>Add this line to the file.  The numbers and stars at the beginning are cron columns that specify when the program should be run - in this case, whenever the minute is 0 and the hour is 1, i.e., 1:00 am every day.</para>
    <programlisting>0 1 * * * /usr/local/pgsql/bin/pg_dump -f /web/service0/database-backup/service0_$(date +%Y-%m-%d).dmp service0</programlisting>
    <para>If you plan to back up the whole <computeroutput>/web/<replaceable>service0</replaceable></computeroutput> directory, then it would be redundant to keep a history of database backups.  In that case, set up the cron job to overwrite the previous backup each time:</para>
    <programlisting>0 1 * * * /usr/local/pgsql/bin/pg_dump -f /web/service0/database-backup/service0_nightly.dmp service0</programlisting>
  </sect2>

  <sect2>
    <title>Set up Log Analysis Reports - OPTIONAL</title>
    
    <para>Analog is a program with processes webserver access logs,
      performs DNS lookup, and outputs HTML reports.  Analog should
      <link linkend="analog-install">already be
      installed.</link>  A modified configuration file is included in
      the OpenACS tarball.</para>
    <orderedlist>
      <listitem>
        <screen>[root@yourserver src]# <userinput>su - service0</userinput>
[service0@yourserver service0]$ <userinput>cd /web/service0</userinput>
[service0@yourserver service0]$ <userinput>cp /web/service0/packages/acs-core-docs/www/files/analog.cfg.txt etc/analog.cfg</userinput>
[service0@yourserver service0]$ <userinput>mkdir www/log</userinput>
[service0@yourserver service0]$ <userinput>cp -r /usr/share/analog-5.31/images www/log/</userinput>
[service0@yourserver service0]$ <action>
su - service0
cd /web/service0
cp /web/service0/packages/acs-core-docs/www/files/analog.cfg.txt etc/analog.cfg
mkdir www/log
cp -r /usr/share/analog-5.31/images www/log/</action></screen>
        <para>Edit
<computeroutput>/web/service0/etc/analog.cfg</computeroutput> and change the variable in <computeroutput>HOSTNAME "[my
organisation]"</computeroutput> to reflect your website title.  If you
don't want the traffic log to be publicly visible, change
<computeroutput>OUTFILE /web/service0/www/log/traffic.html</computeroutput> to use a private
directory.</para>
      </listitem>
      <listitem>
        <para>Run it.</para>
        <screen>[service0@yourserver service0]$ <userinput>/usr/share/analog-5.31/analog -G -g/web/service0/etc/analog.cfg</userinput>
/usr/share/analog-5.31/analog: analog version 5.31/Unix
/usr/share/analog-5.31/analog: Warning F: Failed to open DNS input file
  /home/service0/dnscache: ignoring it
  (For help on all errors and warnings, see docs/errors.html)
/usr/share/analog-5.31/analog: Warning R: Turning off empty Search Word Report
[service0@yourserver service0]$</screen>
        <para>Verify that it works by browing to <computeroutput>http://yourserver.test:8000/log/traffic.html</computeroutput></para>
      </listitem>
      <listitem>
        <para>Automate this by creating a file in
          <computeroutput>/etc/cron.daily</computeroutput>.</para>
          <screen>[service0@yourserver service0]$ <userinput>exit</userinput>
logout

[root@yourserver root]# <userinput>emacs /etc/cron.daily/analog</userinput></screen>
        <para>Put this into the file:</para>
        <programlisting>#!/bin/sh

/usr/share/analog-5.31/analog -G -g/web/<replaceable>service0</replaceable>/etc/analog.cfg</programlisting>
        <screen>[root@yourserver root]# <userinput>chmod 755 /etc/cron.daily/analog</userinput></screen>
        <para>Test it by running the script.</para>
        <screen>[root@yourserver root]# <userinput>sh /etc/cron.daily/analog</userinput></screen>
        <para>Browse to <computeroutput>http://<replaceable>yourserver.test</replaceable>/log/traffic.html</computeroutput></para>
      </listitem>
      
    </orderedlist>
  </sect2>
  <sect2>
    <title>Next Steps</title>
    <itemizedlist>
      <listitem><para>Test your <link
      linkend="backup-recovery">backup and recovery</link> procedure.</para>
      </listitem>
      <listitem><para>Follow the instruction on the home page to change the appearance of your service or add more packages.</para>
      </listitem>
      <listitem><para>Proceed to the <link linkend="tutorial">tutorial</link> to learn how to develop your own packages.</para>
      </listitem>
    </itemizedlist>

  </sect2>

  <para><phrase role="cvstag">($Id$)</phrase></para>
</sect1>
<!--
   Local Variables:
   sgml-parent-document: ("../index.xml" "book" "sect1")
   End:
-->
